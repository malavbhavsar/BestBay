<% devise_error_messages! %>
<% [:success, :info, :notice, :error, :alert, :recaptcha_error].each do |level| %>
    <% unless flash.nil? or flash[level].blank? %>
        <div class="alert alert-<%= flash_class(level) %>">
          <button type="button" class="close" data-dismiss="alert">×</button>
          <%= flash[level] %>
        </div>
    <% end %>
<% end %>